<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
    <title>Home</title>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
</head>
<body>
    <!-- Header dengan Background -->
    <header class="bg-primary text-white text-center py-5">
        <div class="container">
            <h1 class="display-4">Welcome to P4 Courses</h1>
            <p class="lead">Temukan kursus terbaik untuk meningkatkan keterampilan Anda.</p>
            <form action="/search" method="GET" class="mt-4">
                <div class="input-group">
                    <input type="text" id="searchInput" name="query" class="form-control" placeholder="Search courses..." value="<%= typeof query !== 'undefined' ? query : '' %>">
                    <div class="input-group-append">
                        <button class="btn btn-light" type="submit">Search</button>
                        <a href="/" class="btn btn-danger ml-2">Reset</a>
                    </div>
                </div>
            </form>
            <div class="marquee-container mt-3">
                <marquee behavior="scroll" direction="left" scrollamount="3">
                    Selamat datang di P4 Courses! Nikmati kursus terbaik dari kami. Daftar sekarang dan mulailah belajar! Semoga harimu menyenangkan!
                </marquee>
            </div>
        </div>
    </header>

    <!-- Daftar Kursus -->
    <div class="container mt-5">
        <h2 class="mb-4">Courses</h2>
        <div class="row" id="coursesRow">
            <% courses.forEach(course => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= course.image_url %>" class="card-img-top" alt="<%= course.title %>">
                        <div class="card-body">
                            <h5 class="card-title"><%= course.title %></h5>
                            <p class="card-text"><%= course.description %></p>
                        </div>
                        <div class="card-footer bg-transparent border-top-0">
                            <a href="/courses/<%= course.id %>" class="btn btn-primary btn-block">View Course</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <script>
        $(document).ready(function() {
            $('#searchInput').on('keyup', function() {
                const query = $(this).val().toLowerCase();
                $.ajax({
                    url: '/search',
                    method: 'GET',
                    data: { query: query },
                    success: function(response) {
                        $('#coursesRow').empty();
                        response.courses.forEach(course => {
                            const card = `
                                <div class="col-md-4 mb-4">
                                    <div class="card h-100 shadow-sm">
                                        <img src="${course.image_url}" class="card-img-top" alt="${course.title}">
                                        <div class="card-body">
                                            <h5 class="card-title">${course.title}</h5>
                                            <p class="card-text">${course.description}</p>
                                        </div>
                                        <div class="card-footer bg-transparent border-top-0">
                                            <a href="/courses/${course.id}" class="btn btn-primary btn-block">View Course</a>
                                        </div>
                                    </div>
                                </div>
                            `;
                            $('#coursesRow').append(card);
                        });
                    },
                    error: function(error) {
                        console.error('Error fetching courses:', error);
                    }
                });
            });
        });
    </script>
</body>
</html>